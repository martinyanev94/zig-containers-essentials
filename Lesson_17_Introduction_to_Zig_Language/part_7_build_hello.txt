const std = @import("std");
const Build = std.build;

pub fn build(b: *Build.Builder) void {
    const exe = b.addExecutable("hello", "src/main.zig");
    exe.setTarget(b.standardReleaseTarget());
    exe.setBuildMode(b.standardReleaseMode());
}
