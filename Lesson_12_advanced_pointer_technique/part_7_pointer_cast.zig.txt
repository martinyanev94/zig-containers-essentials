const std = @import("std");

pub fn main() void {
    const buffer: [8]u8 = [8]u8{0, 1, 2, 3, 4, 5, 6, 7};
    var ptr: *u8 = &buffer[0];

    const intPtr: *u32 = @ptrCast(*u32, ptr); // Cast pointer to u32
    std.debug.print("First int from buffer: {}\n", .{*intPtr});
}
