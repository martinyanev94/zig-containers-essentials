const std = @import("std");

fn printTypeSize(comptime T: type) void {
    const size = @sizeOf(T);
    std.debug.print("Size of {} is: {}\n", .{@typeName(T), size});
}

pub fn main() void {
    printTypeSize(u32);
    printTypeSize(f64);
    printTypeSize(bool);
}
