// Integer
let apples = 5;

// Floating-point
let pricePerApple = 0.75;

// Character
let firstLetter = 'A';

// String
let message = "I have " + apples + " apples.";

// Output the message
console.log(message);
console.log("Price per apple is: $" + pricePerApple);
